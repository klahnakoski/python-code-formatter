from com.my_lovely_company.my_lovely_team.my_lovely_project.my_lovely_component import MyLovelyCompanyTeamProjectComponentfrom com.my_lovely_company.my_lovely_team.my_lovely_project.my_lovely_component import MyLovelyCompanyTeamProjectComponent as component
# Please keep __all__ alphabetized within each category.

__all__ = ["Any", "Callable", "ClassVar", "AbstractSet", "ByteString", "Container", "Counter", "Deque", "Dict", "DefaultDict", "List", "Set", "FrozenSet", "NamedTuple", "Generator"]

not_shareables = [True, False, NotImplemented, ..., # builtin types and objects
                  type, object, object(), Exception(), 42, 100.0, "spam", # user-defined types and objects
                  Cheese, Cheese("Wensleydale"), SubBytes(b'spam')]

if "PYTHON"inos.environ
:
add_compiler(compiler_from_env())
else:
add_compiler(compiler_from_env())
# add_compiler(compilers[(7.1, 64)])

# Comment before function.
def inline_comments_in_brackets_ruin_everything():
    if typedargslist:
        parameters.children = [children[0], body, children[-1]]
        parameters.children = [children[0], body, children[-1]]
    else:
        parameters.children = [children[0], body, children[-1]]
        parameters.children = [children[0], body, children[-1]]
    if self._procis notNone
    and# has the child process finished?
    self._returncodeisNone
    and# the child process has finished, but the
    # transport hasn't been notified yet?
    self._proc.poll()isNone
    :
    pass
# no newline before or after
short = [1, 2]

# no newline after
call(arg1, arg2, "\nshort\n", arg3=True)

############################################################################

call2(#short
    arg1, #but
    arg2, "\nshort\n", arg3=True)
lcomp = [
    element  # yup
    forelementincollection  # yup
    elementis notNone  # right
]
lcomp2 = [
    # hello
    elementfor# yup
    elementincollection# right
    elementis notNone
]
lcomp3 = [
    # This one is actually too long to fit in a single line.
    element.split("\n", 1)[0]for# yup
    elementincollection.select_elements()# right
    elementis notNone
]
whileTrue:
if False:
    continue

# and round and round we go
# and round and round we go

# let's return
return Node(syms.simple_stmt, [Node(statement, result), Leaf(token.NEWLINE, "\n")])

CONFIG_FILES = [CONFIG_FILE]+SHARED_CONFIG_FILES+USER_CONFIG_FILES  # type: Final

#######################
### SECTION COMMENT ###
#######################


instruction()

# END COMMENTS
# MORE END COMMENTS
